<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ddd.infrastructure.dao.mapper.AgentMapper">

    <resultMap id="BaseResultMap" type="com.example.ddd.infrastructure.dao.po.AgentPO">
        <id column="id" property="id" jdbcType="VARCHAR"/>
        <result column="orchestrator_id" property="orchestratorId" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="BIGINT"/>
        <result column="updated_at" property="updatedAt" jdbcType="BIGINT"/>
        <result column="system_prompt" property="systemPrompt" jdbcType="VARCHAR"/>
        <result column="role" property="role" jdbcType="INTEGER" typeHandler="com.example.ddd.infrastructure.dao.typehandler.AgentRoleTypeHandler"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, orchestrator_id, name, description, status, created_at, updated_at, system_prompt, role
    </sql>

    <insert id="insert" parameterType="com.example.ddd.infrastructure.dao.po.AgentPO">
        INSERT INTO agent (id, name, description, status, created_at, updated_at, system_prompt)
        VALUES (#{id}, #{name}, #{description}, #{status}, #{createdAt}, #{updatedAt}, #{systemPrompt})
    </insert>

    <select id="queryById" resultMap="BaseResultMap">
        SELECT a.id,
               (SELECT oa.orchestrator_id FROM orchestrator_agent oa WHERE oa.agent_id = a.id LIMIT 1) as orchestrator_id,
               a.name, a.description, a.status, a.created_at, a.updated_at, a.system_prompt,
               (SELECT oa.role FROM orchestrator_agent oa WHERE oa.agent_id = a.id LIMIT 1) as role
        FROM agent a
        WHERE a.id = #{id}
    </select>

    <select id="queryByName" resultMap="BaseResultMap">
        SELECT a.id,
               (SELECT oa.orchestrator_id FROM orchestrator_agent oa WHERE oa.agent_id = a.id LIMIT 1) as orchestrator_id,
               a.name, a.description, a.status, a.created_at, a.updated_at, a.system_prompt,
               (SELECT oa.role FROM orchestrator_agent oa WHERE oa.agent_id = a.id LIMIT 1) as role
        FROM agent a
        WHERE a.name = #{name}
    </select>

    <select id="queryAll" resultMap="BaseResultMap">
        SELECT a.id,
               (SELECT oa.orchestrator_id FROM orchestrator_agent oa WHERE oa.agent_id = a.id LIMIT 1) as orchestrator_id,
               a.name, a.description, a.status, a.created_at, a.updated_at, a.system_prompt,
               (SELECT oa.role FROM orchestrator_agent oa WHERE oa.agent_id = a.id LIMIT 1) as role
        FROM agent a
    </select>

    <select id="queryByStatus" resultMap="BaseResultMap">
        SELECT a.id,
               (SELECT oa.orchestrator_id FROM orchestrator_agent oa WHERE oa.agent_id = a.id LIMIT 1) as orchestrator_id,
               a.name, a.description, a.status, a.created_at, a.updated_at, a.system_prompt,
               (SELECT oa.role FROM orchestrator_agent oa WHERE oa.agent_id = a.id LIMIT 1) as role
        FROM agent a
        WHERE a.status = #{status}
    </select>

    <update id="update" parameterType="com.example.ddd.infrastructure.dao.po.AgentPO">
        UPDATE agent
        SET name = #{name},
            description = #{description},
            status = #{status},
            updated_at = #{updatedAt},
            system_prompt = #{systemPrompt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM agent WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM agent WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="queryByOrchestratorId" resultMap="BaseResultMap">
        SELECT a.id,
               oa.orchestrator_id,
               a.name, a.description, a.status, a.created_at, a.updated_at, a.system_prompt,
               oa.role
        FROM agent a
        INNER JOIN orchestrator_agent oa ON a.id = oa.agent_id
        WHERE oa.orchestrator_id = #{orchestratorId}
        ORDER BY a.created_at
    </select>

    <select id="queryAgentIdsByOrchestratorId" resultType="string">
        SELECT oa.agent_id
        FROM orchestrator_agent oa
        WHERE oa.orchestrator_id = #{orchestratorId}
        ORDER BY oa.seq
    </select>

</mapper>

