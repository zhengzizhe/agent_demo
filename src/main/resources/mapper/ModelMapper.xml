<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ddd.infrastructure.dao.mapper.ModelMapper">

    <resultMap id="BaseResultMap" type="com.example.ddd.infrastructure.dao.po.ModelPO">
        <id column="id" property="id" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="provider" property="provider" jdbcType="VARCHAR"/>
        <result column="model_type" property="modelType" jdbcType="VARCHAR"/>
        <result column="api_endpoint" property="apiEndpoint" jdbcType="VARCHAR"/>
        <result column="api_key" property="apiKey" jdbcType="VARCHAR"/>
        <result column="max_tokens" property="maxTokens" jdbcType="INTEGER"/>
        <result column="temperature" property="temperature" jdbcType="DOUBLE"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="BIGINT"/>
        <result column="updated_at" property="updatedAt" jdbcType="BIGINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, provider, model_type, api_endpoint, api_key, max_tokens, temperature, status, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.example.ddd.infrastructure.dao.po.ModelPO">
        INSERT INTO model (id, name, provider, model_type, api_endpoint, api_key, max_tokens, temperature, status, created_at, updated_at)
        VALUES (#{id}, #{name}, #{provider}, #{modelType}, #{apiEndpoint}, #{apiKey}, #{maxTokens}, #{temperature}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="queryById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM model
        WHERE id = #{id}
    </select>

    <select id="queryByName" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM model
        WHERE name = #{name}
    </select>

    <select id="queryByProvider" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM model
        WHERE provider = #{provider}
    </select>

    <select id="queryAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM model
    </select>

    <select id="queryByStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM model
        WHERE status = #{status}
    </select>

    <select id="queryByAgentId" resultMap="BaseResultMap">
        SELECT m.<include refid="Base_Column_List"/>
        FROM model m
        INNER JOIN agent_model am ON m.id = am.model_id
        WHERE am.agent_id = #{agentId}
    </select>

    <select id="queryByClientId" resultMap="BaseResultMap">
        SELECT DISTINCT m.<include refid="Base_Column_List"/>
        FROM model m
        INNER JOIN agent_model am ON m.id = am.model_id
        WHERE am.agent_id = #{clientId}
    </select>

    <select id="queryByClientIds" resultMap="BaseResultMap">
        SELECT DISTINCT m.<include refid="Base_Column_List"/>
        FROM model m
        INNER JOIN agent_model am ON m.id = am.model_id
        WHERE am.agent_id IN
        <foreach collection="clientIds" item="clientId" open="(" separator="," close=")">
            #{clientId}
        </foreach>
    </select>

    <update id="update" parameterType="com.example.ddd.infrastructure.dao.po.ModelPO">
        UPDATE model
        SET name = #{name},
            provider = #{provider},
            model_type = #{modelType},
            api_endpoint = #{apiEndpoint},
            api_key = #{apiKey},
            max_tokens = #{maxTokens},
            temperature = #{temperature},
            status = #{status},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM model WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM model WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>

